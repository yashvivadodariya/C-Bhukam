#include<stdio.h>
#include<conio.h>

main()
{
    char n;
    clrscr();
    printf("Enter the charcter :");
    scanf("%c",&n);
    switch(n)
    {
      case 'a':
      printf("%c is vowel",n);
      break;

      case 'e':
      printf("%c is vowel",n);
      break;

      case 'i':
      printf("%c is vowel ",n);
      break;

      case'o' :
      printf("%c is vowel ",n);
      break;

      case 'u':
      printf("%c is vowel ",n);
      break;

      case
      printf("%c is console",n);
      break;
    }

    getch();



}